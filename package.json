{
  "name": "a5cforge",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "devDependencies": {
    "@types/node": "^22.10.2"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present build && pnpm -r test",
    "coverage": "pnpm -r --if-present build && pnpm -r --if-present test:coverage"
  }
}


