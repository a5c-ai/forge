cli:
  echo:
    description: "fixture: local echo agent (writes output_last_message_path)"
    cli_command: "node .a5c/agents/echo-agent.js"

profiles:
  default:
    default: true
    description: "fixture default agent profile"
    cli: echo
    model: "fixture"
    cli_params: "--prompt {{prompt_path}} --out {{output_last_message_path}}"

